1.类型确定。每一个不符合类型的情况都要进行强行退出。
2.每一个类型都要有一个独立的ft，然后在主程序中整合。
3.所有的类型都要在char *type之内进行判断。结合第一点双向保证。

check
if type[0] == '%'
	if type[1] = 'c'
		ft_c;
	......
returen ;

注意：每个方程的错误并没有完全检查

有几个错误需要检查：
1.type里如果输入超过两个参数需要报错
2.每一个具体类型之中都需要再次检查。如果和输入的类型不匹配需要报错
3.在list a之中如果输入多于两个参数需要报错（这个暂时并不确定）
4.如果没有输入任何东西需要报错，包括list和type

今日经过fra的检查发现的问题：
1.x的理解有问题。和正常的printf并不一样。
